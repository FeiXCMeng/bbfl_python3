# -*- coding:utf-8 -*-
formatter = "{} {} {} {}"
print(formatter.format(1, 2, 3, 4))
print(formatter.format("today", "do", "not", "snow"))
print(formatter.format(formatter, formatter, formatter, 666))
print(formatter.format(
    "nanjing",
    "is",
    "better than",
    "hangzhou?"
))
